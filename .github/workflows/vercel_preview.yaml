name: GitHub Actions Vercel Preview Deployment
env:
  VERCEL_ORG_ID: DzHjaBMPRs5MdmEA7540OC5D
  VERCEL_PROJECT_ID: prj_OzH2Q59u6LIrjAM2HjuLwGUT88Cv
on:
  workflow_dispatch:
  push:
    branches:
      - test
jobs:
  Deploy-Preview:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Install Vercel CLI
        run: npm install --global vercel@canary
      - name: deploy
        run: vercel deploy
#      - name: Pull Vercel Environment Information
#        run: vercel pull --yes --environment=preview --token=DzHjaBMPRs5MdmEA7540OC5D
#      - name: Build Project Artifacts
#        run: vercel build --token=DzHjaBMPRs5MdmEA7540OC5D
#      - name: Deploy Project Artifacts to Vercel
#        run: vercel deploy --prebuilt --token=DzHjaBMPRs5MdmEA7540OC5D
